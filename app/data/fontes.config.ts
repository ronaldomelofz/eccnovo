/**
 * Referência das fontes oficiais do projeto.
 * Os dados reais são lidos por scripts/fontes.js — execute `npm run sync`.
 */
export const FONTES_PROJETO = {
  /** Pasta na raiz onde o usuário coloca fotos e áudio */
  fotos: 'FOTOS',
  /** Planilha na raiz com listagem dos encontros */
  agenda: 'AGENDA ENCONTROS.xlsx',
  /** Comando para sincronizar tudo */
  comandoSync: 'npm run sync',
} as const
